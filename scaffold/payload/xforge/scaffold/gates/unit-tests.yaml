apiVersion: xforge.dev/v1alpha1
kind: Gate
metadata:
  name: unit-tests
  version: 1
spec:
  required: true
  command: [npm, test, "--if-present"]
  workingDirectory: .
  timeoutSeconds: 900
  maxOutputBytes: 65536
  evidence: tests.json
