code: XF-ASSEMBLE-014
title: "执法钩子跑不起来"
meaning: "钩子已经装进宿主原生位置，但它的命令在**跑 doctor 的这个进程**的 PATH 上解析不到。钩子是宿主进程去起的、用的是宿主的 PATH，两边不一样是常态 —— 所以这是提醒不是判决。如果宿主那边也找不到，它会照常发起钩子、拿不到决策然后继续执行。"
remedy:
  text: "先确认宿主那边找不找得到它；确实找不到就把 CLI 装到宿主进程也看得见的 PATH 上（npm i -g @xforge/cli），或把钩子声明里的命令改成绝对路径。"
variants: []
