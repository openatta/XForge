$ xforge doctor
exit: 0

{
  "protocolVersion": "2",
  "ok": true,
  "command": "doctor",
  "root": "<ROOT>",
  "data": {
    "kind": "all",
    "summary": {
      "dangling": 0,
      "deadCode": 0,
      "uncited": 0,
      "unusedFlows": 2,
      "unusableApprovals": 0,
      "conformance": 0,
      "suggestions": 8
    },
    "danglingReferences": [],
    "deadCode": [],
    "uncited": [],
    "unusedFlows": [
      {
        "scope": "flows",
        "code": "XFORGE_DOCTOR_UNUSED_FLOW",
        "id": "major",
        "message": "Flow major is not the Manifest default and is not used by any active Change.",
        "path": "xforge/flows/major.yaml"
      },
      {
        "scope": "flows",
        "code": "XFORGE_DOCTOR_UNUSED_FLOW",
        "id": "quick",
        "message": "Flow quick is not the Manifest default and is not used by any active Change.",
        "path": "xforge/flows/quick.yaml"
      }
    ],
    "unusableApprovals": [],
    "conformance": [],
    "suggestions": [
      {
        "scope": "skills",
        "code": "XFORGE_DOCTOR_ARCHITECTURE_ABSENT",
        "id": "xforge-architect",
        "message": "This project has no xforge/architecture.md, so each Change designs without a durable record of the decisions the last one made. Run the xforge-architect Skill to write one — from the existing code, by answering a few questions, or from a description. It is a suggestion, not a requirement: nothing is blocked without it.",
        "path": "xforge/architecture.md",
        "severity": "info"
      },
      {
        "scope": "policies",
        "code": "XFORGE_DOCTOR_POLICY_EXEMPTION_UNENFORCEABLE",
        "id": "protected-files",
        "message": "PermissionPolicy protected-files exempts integrator, and none of this project's targets (codex, claude, cursor, opencode, github-copilot) can express an actor exemption in its own permission layer. The exemption therefore lives only in the XForge runtime Hook bridge, which applies it only when the host reports the calling sub-agent's identity — so wherever it does not, this policy is an unconditional deny for every actor including the exempted one. Confirm the exempted actors are registered sub-agents of a target that reports them, or write the policy so it does not rely on an exemption.",
        "path": "xforge/scaffold/policies/protected-files.yaml",
        "severity": "info"
      },
      {
        "scope": "policies",
        "code": "XFORGE_DOCTOR_POLICY_EXEMPTION_UNENFORCEABLE",
        "id": "protected-manifest",
        "message": "PermissionPolicy protected-manifest exempts integrator, and none of this project's targets (codex, claude, cursor, opencode, github-copilot) can express an actor exemption in its own permission layer. The exemption therefore lives only in the XForge runtime Hook bridge, which applies it only when the host reports the calling sub-agent's identity — so wherever it does not, this policy is an unconditional ask for every actor including the exempted one. Confirm the exempted actors are registered sub-agents of a target that reports them, or write the policy so it does not rely on an exemption.",
        "path": "xforge/scaffold/policies/protected-manifest.yaml",
        "severity": "info"
      },
      {
        "scope": "policies",
        "code": "XFORGE_DOCTOR_HOOK_SUPPRESSION_UNVERIFIABLE",
        "message": "This project's PermissionPolicies are enforced by Hooks, and its targets declare these ways Hooks can be evaded — codex: hosted tools; specialized opt-out tool paths | claude: disableAllHooks in any settings file; allowManagedHooksOnly in managed settings | cursor: events not exposed by Cursor | opencode: events not emitted by the plugin API | github-copilot: events not exposed by Copilot hooks. XForge can observe one of them: disableAllHooks in this project's own Claude settings file, which is reported separately when it is set. It cannot observe user-level settings, administrator-managed settings, or settings a host fetches from a server at startup, because none of those sit inside the project root. A Hook suppressed by any of them leaves no in-band signal. Ask the host instead: `claude doctor` lists managed settings it dropped, and /status names the setting sources actually in effect.",
        "severity": "info"
      },
      {
        "scope": "rules",
        "code": "XFORGE_DOCTOR_RULE_SCOPE_EMPTY",
        "id": "observable-requirements-are-tested",
        "message": "Rule observable-requirements-are-tested is scoped to src/**, tests/**, which matches no file in this repository. XForge compares that list with the paths a Change declares in change.yaml, and the installed Adapters hand it to the host as a file matcher; on this layout both come up empty, so the Rule is registered, enforceable, and reaching nothing. Rewrite scope.paths to the paths this repository actually uses, or drop it to have the Rule apply everywhere.",
        "path": "xforge/scaffold/rules/observable-requirements-are-tested.yaml",
        "severity": "info"
      },
      {
        "scope": "rules",
        "code": "XFORGE_DOCTOR_RULE_SCOPE_EMPTY",
        "id": "prefer-small-explicit-contracts",
        "message": "Rule prefer-small-explicit-contracts is scoped to src/**, which matches no file in this repository. XForge compares that list with the paths a Change declares in change.yaml, and the installed Adapters hand it to the host as a file matcher; on this layout both come up empty, so the Rule is registered, enforceable, and reaching nothing. Rewrite scope.paths to the paths this repository actually uses, or drop it to have the Rule apply everywhere.",
        "path": "xforge/scaffold/rules/prefer-small-explicit-contracts.yaml",
        "severity": "info"
      },
      {
        "scope": "rules",
        "code": "XFORGE_DOCTOR_RULE_SCOPE_EMPTY",
        "id": "design-within-the-declared-architecture",
        "message": "Rule design-within-the-declared-architecture is scoped to src/**, which matches no file in this repository. XForge compares that list with the paths a Change declares in change.yaml, and the installed Adapters hand it to the host as a file matcher; on this layout both come up empty, so the Rule is registered, enforceable, and reaching nothing. Rewrite scope.paths to the paths this repository actually uses, or drop it to have the Rule apply everywhere.",
        "path": "xforge/scaffold/rules/design-within-the-declared-architecture.yaml",
        "severity": "info"
      },
      {
        "scope": "approvals",
        "code": "XFORGE_DOCTOR_APPROVALS_INTERACTIVE_ONLY",
        "id": "local",
        "message": "2 approval policies in the Flows this project uses can be satisfied only at an interactive terminal, because no mcp provider is both declared and configured for them: solid/planning-solid, solid/closing-solid. Those approvals cannot be collected from inside an Agent session — the approver has to open a real terminal each time. That is a working constraint, not a misconfiguration; the alternative is to configure an mcp approval provider. See docs/extension-guide.md.",
        "path": "xforge/manifest.yaml",
        "severity": "info"
      }
    ]
  },
  "diagnostics": [
    {
      "code": "XFORGE_DOCTOR_UNUSED_FLOW",
      "severity": "warning",
      "message": "Flow major is not the Manifest default and is not used by any active Change.",
      "path": "xforge/flows/major.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_UNUSED_FLOW",
      "severity": "warning",
      "message": "Flow quick is not the Manifest default and is not used by any active Change.",
      "path": "xforge/flows/quick.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_ARCHITECTURE_ABSENT",
      "severity": "info",
      "message": "This project has no xforge/architecture.md, so each Change designs without a durable record of the decisions the last one made. Run the xforge-architect Skill to write one — from the existing code, by answering a few questions, or from a description. It is a suggestion, not a requirement: nothing is blocked without it.",
      "path": "xforge/architecture.md"
    },
    {
      "code": "XFORGE_DOCTOR_POLICY_EXEMPTION_UNENFORCEABLE",
      "severity": "info",
      "message": "PermissionPolicy protected-files exempts integrator, and none of this project's targets (codex, claude, cursor, opencode, github-copilot) can express an actor exemption in its own permission layer. The exemption therefore lives only in the XForge runtime Hook bridge, which applies it only when the host reports the calling sub-agent's identity — so wherever it does not, this policy is an unconditional deny for every actor including the exempted one. Confirm the exempted actors are registered sub-agents of a target that reports them, or write the policy so it does not rely on an exemption.",
      "path": "xforge/scaffold/policies/protected-files.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_POLICY_EXEMPTION_UNENFORCEABLE",
      "severity": "info",
      "message": "PermissionPolicy protected-manifest exempts integrator, and none of this project's targets (codex, claude, cursor, opencode, github-copilot) can express an actor exemption in its own permission layer. The exemption therefore lives only in the XForge runtime Hook bridge, which applies it only when the host reports the calling sub-agent's identity — so wherever it does not, this policy is an unconditional ask for every actor including the exempted one. Confirm the exempted actors are registered sub-agents of a target that reports them, or write the policy so it does not rely on an exemption.",
      "path": "xforge/scaffold/policies/protected-manifest.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_HOOK_SUPPRESSION_UNVERIFIABLE",
      "severity": "info",
      "message": "This project's PermissionPolicies are enforced by Hooks, and its targets declare these ways Hooks can be evaded — codex: hosted tools; specialized opt-out tool paths | claude: disableAllHooks in any settings file; allowManagedHooksOnly in managed settings | cursor: events not exposed by Cursor | opencode: events not emitted by the plugin API | github-copilot: events not exposed by Copilot hooks. XForge can observe one of them: disableAllHooks in this project's own Claude settings file, which is reported separately when it is set. It cannot observe user-level settings, administrator-managed settings, or settings a host fetches from a server at startup, because none of those sit inside the project root. A Hook suppressed by any of them leaves no in-band signal. Ask the host instead: `claude doctor` lists managed settings it dropped, and /status names the setting sources actually in effect."
    },
    {
      "code": "XFORGE_DOCTOR_RULE_SCOPE_EMPTY",
      "severity": "info",
      "message": "Rule observable-requirements-are-tested is scoped to src/**, tests/**, which matches no file in this repository. XForge compares that list with the paths a Change declares in change.yaml, and the installed Adapters hand it to the host as a file matcher; on this layout both come up empty, so the Rule is registered, enforceable, and reaching nothing. Rewrite scope.paths to the paths this repository actually uses, or drop it to have the Rule apply everywhere.",
      "path": "xforge/scaffold/rules/observable-requirements-are-tested.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_RULE_SCOPE_EMPTY",
      "severity": "info",
      "message": "Rule prefer-small-explicit-contracts is scoped to src/**, which matches no file in this repository. XForge compares that list with the paths a Change declares in change.yaml, and the installed Adapters hand it to the host as a file matcher; on this layout both come up empty, so the Rule is registered, enforceable, and reaching nothing. Rewrite scope.paths to the paths this repository actually uses, or drop it to have the Rule apply everywhere.",
      "path": "xforge/scaffold/rules/prefer-small-explicit-contracts.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_RULE_SCOPE_EMPTY",
      "severity": "info",
      "message": "Rule design-within-the-declared-architecture is scoped to src/**, which matches no file in this repository. XForge compares that list with the paths a Change declares in change.yaml, and the installed Adapters hand it to the host as a file matcher; on this layout both come up empty, so the Rule is registered, enforceable, and reaching nothing. Rewrite scope.paths to the paths this repository actually uses, or drop it to have the Rule apply everywhere.",
      "path": "xforge/scaffold/rules/design-within-the-declared-architecture.yaml"
    },
    {
      "code": "XFORGE_DOCTOR_APPROVALS_INTERACTIVE_ONLY",
      "severity": "info",
      "message": "2 approval policies in the Flows this project uses can be satisfied only at an interactive terminal, because no mcp provider is both declared and configured for them: solid/planning-solid, solid/closing-solid. Those approvals cannot be collected from inside an Agent session — the approver has to open a real terminal each time. That is a working constraint, not a misconfiguration; the alternative is to configure an mcp approval provider. See docs/extension-guide.md.",
      "path": "xforge/manifest.yaml"
    }
  ],
  "changes": [],
  "nextActions": []
}
