# live 基线 · 2026-09-19（provider 抽象与装配五命令之后）

> `docs/live-baseline-1.0.0.md` 原样保留，**不要改它**：它是 2026-09-17 那一天的记录，
> 跨天对比要靠两份都在。这一份是 2026-09-19 的同日记录，以后对比引用它。
>
> 这一天的七场里有一场是**对照组**：用 1.0.1 的代码（`58e6dec`）在同一天、同一条链路上跑，
> 专门用来把「代码的影响」与「模型与链路的当日状态」分开。结论见 §3。

## 1. 条件

| 项 | 值 |
| --- | --- |
| 引擎 A | 本机 Claude Code 凭据，`claude -p`，模型 `claude-haiku-4-5-20251001+claude-opus-5[1m]`（与 1.0.0 基线**同一个字符串**），走本机代理 |
| 引擎 B | 仓库 `.env` 的 Anthropic 兼容网关，模型 `deepseek-v4-flash`，不走代理直连 |
| 治理组合 | 全部 `TT`（规格与接口治理都开）—— 这轮改动在装配层与门的执行环境，与治理开关无关，取最严的一档 |
| 驱动 | 全部 `orchestrated` |
| 语言 | Skill 中文投影（`zh-CN`） |
| oracle | quick 5 条、solid 7 条、major 7 条。**1.0.0 基线文档里那批运行用的是加强前的 3/4/4**，那一列不可直接比 |
| 验收 | 归档后跑 oracle、`xforge inspect --hygiene`、篡改一条 receipt 验 `inspect` 报链断；另加装配体检（`LT-09`/`LT-10`/`LT-11`） |

被测提交：

| 提交 | 是什么 |
| --- | --- |
| `58e6dec` | XForge 1.0.1，provider 工作之前。**对照组** |
| `854dceb` | 五命令 + provider 抽象 + 两轮修复 + 全量 review 的十三处 |
| `ad2d787` | 在 `854dceb` 之上修掉 `XF-ASSEMBLE-014`（见 §4） |

## 2. 七场

| 引擎 | 提交 | 场景 | 结果 | 外层轮 | 内部轮 | 返工 | oracle | input | output | cache_read | cache_creation | xforge 调用 | show | 直读 changes/** | 工具调用 | 执法 deny | inspect | 篡改检测 | 用时（分） | **output/分** | 人的动作 | Requirement 覆盖 | 装配体检 | 运行目录 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| A | `58e6dec` | quick | archived | 2 | 35 | 0 | 5/5 | 68 | 22093 | 1365216 | 75307 | 35 | 0 | 9 | 60 | 0 | 0 | 退出码 3，XF-INSPECT-001 | 8.5 | **2594** | 7 | 2/2 | —（1.0.1 没这段） | `2026-09-19T06-38-24-864Z` |
| A | `854dceb` | quick | archived | 2 | 26 | 0 | 5/5 | 48 | 17480 | 709014 | 65488 | 36 | 3 | 7 | 52 | 0 | 0 | 退出码 3，XF-INSPECT-001 | 8.1 | **2147** | 7 | 3/3 | 假 `014`（见 §4） | `2026-09-19T05-00-35-586Z` |
| A | `ad2d787` | quick | archived | 2 | 22 | 0 | 5/5 | 42 | 18992 | 631655 | 57751 | 37 | 2 | 6 | 53 | 0 | 0 | 退出码 3，XF-INSPECT-001 | 8.7 | **2172** | 7 | 2/2 | 干净 | `2026-09-19T05-32-57-843Z` |
| A | `854dceb` | solid-rework | **stalled**（design） | 5 | 51 | 1 | —（没跑到） | 100 | 38746 | 1543500 | 104405 | 95 | 7 | 19 | 112 | 0 | 0 | — | 26.0 | **1488** | 1 | — | 假 `014` | `2026-09-19T05-00-35-984Z` |
| A | `854dceb` | major | archived | 5 | 59 | 0 | 7/7 | 112 | 43254 | 1825400 | 141959 | 103 | 4 | 24 | 133 | 0 | 0 | 退出码 3，XF-INSPECT-001 | 29.4 | **1469** | 24 | 7/7 | 假 `014` | `2026-09-19T05-00-34-830Z` |
| B | `ad2d787` | quick | archived | 2 | 45 | 0 | 5/5 | 68598 | 34047 | 1284864 | 0 | 60 | 8 | 7 | 118 | 1（实为 1，见 §5） | 0 | 退出码 3，XF-INSPECT-001 | 12.7 | **2673** | 7 | 3/3 | 干净 | `2026-09-19T05-42-55-320Z` |
| B | `ad2d787` | solid | archived | 3 | 49 | 0 | 7/7 | 90499 | 33295 | 1346688 | 0 | 89 | 9 | 39 | 215 | 0 | 0 | 退出码 3，XF-INSPECT-001 | 21.7 | **1531** | 10 | 5/5 | 干净 | `2026-09-19T05-42-55-674Z` |

六场归档、一场停滞（原因见 §6）。执法 deny 除 B 的那一次真实拦截外全零；`inspect` 全 0；篡改检测全部报链断；治理不变量（`TT` 要求两条基线都动）在每一场归档里都对。

## 3. 对照组说明了什么

同一天、同一条链路，只有代码不同：

| | 文档基线 09-17 | **今天 `58e6dec`（1.0.1）** | 今天 `854dceb` | 今天 `ad2d787` |
| --- | ---: | ---: | ---: | ---: |
| 外层轮 | 4 | **2** | 2 | 2 |
| output | 17 640 | **22 093** | 17 480 | 18 992 |
| xforge 调用 | 32 | **35** | 36 | 37 |
| 工具调用 | 39 | **60** | 52 | 53 |
| output/分 | 3556 | **2594** | 2147 | 2172 |

**1.0.1 的代码今天也是 35 次调用、60 次工具、2594 output/分**，与今天我们的代码同档，而与 09-17 的 32 / 39 / 3556 明显不同。所以：

- **工具调用从 39 涨到 50–60，不是这轮改动引入的**；1.0.1 今天比我们还高。
- **吞吐腰斩（3500 → 2100–2600 output/分）也不是**；模型字符串与 09-17 完全相同，所以不是换了模型，是服务端与链路的当日状态。
- 在这一对里，`ad2d787` 的 output 比 1.0.1 **低 14%**。

两处让 09-17 与今天不可直接比的原因，都与这轮改动无关：

1. **oracle 条件**：文档那批用 3/4/4，1.0.1 起已是 5/7/7。文档的数字比它自己的仓库状态旧。
2. **当日链路状态**：同样的 1.0.1 代码，09-17 是 32 次 / 3556，今天是 35 次 / 2594。

> **教训**：跨天比 live 数字要先有同日对照组。单看「今天 vs 某天的文档」永远分不清代码与环境。

## 4. `XF-ASSEMBLE-014` 的假阳性（`854dceb` → `ad2d787` 修掉）

`854dceb` 的三场都报「钩子装上了但跑不起来」，而那三棵树上钩子一直在跑。
原因：那条检查拿**自己进程的 PATH** 去替**宿主进程**回答。live harness 正是两边不同的形状
（模型会话的 PATH 里有 shim 目录，harness 调 `doctor` 的那一份没有）。

`ad2d787` 之后 `enforcement` 只看钩子在不在宿主原生位置里（验得出来的那一半），`014` 降为 `warning`
且不参与判定。此后三场（A 一场 + B 两场）的装配体检都是：

```json
{"codes": [], "exit": 0, "wrote": [], "repair": {"changed": 2, "leftCodes": [], "exit": 0}}
```

`LT-09`（真树上装配干净）、`LT-10`（`doctor` 不写盘）、`LT-11`（注入漂移被 `repair` 收敛）**首次在真机验证通过**。

## 5. B 的那次 deny 是执法在正确工作

执行者把交付台账写到了 `<project>/ledgers/deliveries/P-01.yaml`（项目根），
而不是 `xforge/changes/<id>/ledgers/deliveries/P-01.yaml`。那个位置不在包投影的 `allow`
（`invoicely/core.py`、`tests/test_core.py`）里，钩子报 `XF-ENFORCE-002` 拦下；模型随后改对，
台账最终落在正确位置。`deepseek-v4-flash` 在路径上比 Claude 弱，0.8.6 同引擎也出现过 3 次。

> **已知 harness 计数 bug（未修）**：`observe()` 对 `tool_use` 去了重，`tool_result` 那一支没有。
> 同一次 deny 同时出现在主会话与子 Agent 转录里会被记成 2。上表那一格写的是实际值 1，
> `summary.json` 里是 2。`tool_uses` 不受影响。

## 6. solid-rework 停滞的原因（产品缺口，非回归）

模型把植入的设计矛盾（R-0003）**修对了**，然后发现了一条真实的规格歧义：
`REQ-billing-payments-003` 的三条 WHEN 不互斥 —— 总额为 0 的新发票同时命中
「没有任何收款 → `open`」与「余额为 0 → `paid`」，条文没给优先级。
执行者按站的停下条件停在 `blocked spec-conflict`。

模型自己说得很准：

> 控制面没有为这个 token 登记 remedy，所以没有现成的解法可念给你。

于是 `state` 的 `blockers` 是**空的** —— 从治理角度确实没有东西挡着。模型连着三轮转向人求裁决，
而 harness 的模拟人只会做脚本化的动作（`attest` / `approve` / `rework`），答不了规格问题，
撞上「位置与阻塞连续三轮不变」的停滞规则。

三方都没做错，缺口是真的：**执行者能停在一个控制面看不见的理由上**，`state` 就给不出可执行的下一步，
自动驱动必然死锁。待排。

## 7. 怎么复现

```sh
# 今天这批（引擎 A，当前代码）
node scripts/live-matrix.mjs --engine A --only quick:TT,solid-rework:TT,major:TT --concurrency 3

# 引擎 B
node scripts/live-matrix.mjs --engine B --only quick:TT,solid:TT --concurrency 2

# 同日对照组：拿某个历史提交的代码跑，唯一变量是代码
node scripts/live-matrix.mjs --engine A --only quick:TT --ref 58e6dec
```

summary 落在 `test/.tmp/live/<claude|gateway>/<场景>/<时间>/summary.{json,md}`。

## 8. v1.0.2 的引擎 B 全矩阵（`23c1bfa`，2026-09-19 下午）

跑 1.0.0 基线里引擎 B 那五场的同一组合，专门看 v1.0.2 三处改动有没有副作用：
design 站新增 `exit/spec-conflicts` 台账与 `ledgers` 门、codex 获得执法能力、
`currentProvider()` 改成优先挑不需要宿主放行的 provider。
五场各自一个 detached worktree 并行跑，结果目录由 `XF_LIVE_RESULTS_ROOT` 收回主仓。

| 场景 | 治理 | 驱动 | 结果 | 外层轮 | 内部轮 | 返工 | oracle | input | output | cache_read | xforge 调用 | show | 直读 changes/** | 工具调用 | 执法 deny | inspect | 用时（分） | 人的动作 | 装配体检 | 运行目录 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| quick | TT | orchestrated | archived | 2 | 50 | 0 | 5/5 | 67192 | 26176 | 1350016 | 59 | 5 | 40 | 174 | 0 | 0 | 10.4 | 7 | 干净 | `2026-09-19T15-35-01-004Z` |
| quick | FF | orchestrated | archived | 3 | 36 | 0 | 5/5 | 84115 | 33699 | 762752 | 63 | 3 | 11 | 111 | 0 | 0 | 14.6 | 8 | 干净 | `2026-09-19T15-35-00-681Z` |
| solid | TT | orchestrated | archived | 4 | 149 | 0 | 7/7 | 208897 | 121033 | 6148608 | 182 | 13 | 45 | 322 | 0 | 0 | 37.1 | 14 | 干净 | `2026-09-19T15-34-58-461Z` |
| solid | FF | orchestrated | archived | 3 | 49 | 0 | 7/7 | 95973 | 42947 | 1207808 | 106 | 8 | 31 | 213 | 0 | 0 | 23.6 | 10 | 干净 | `2026-09-19T15-34-59-475Z` |
| solid | FF | stepwise | archived | 8 | 419 | 0 | 7/7 | 468333 | 291217 | 15355136 | 337 | 6 | 43 | 486 | 0 | 8 | 62.5 | 10 | 干净 | `2026-09-19T15-35-01-363Z` |

五场全部 `archived`、退出 0、返工 0、oracle 全过、篡改检测全部退出码 3 报 `XF-INSPECT-001`。
「装配体检」干净的意思是：`doctor` 非 info 的码为 0、退出 0、**没有写盘**（`LT-10`），
注入一处漂移后 `repair` 改回 2 个文件、修完无残留（`LT-11`）。

### 对 1.0.0 基线（同引擎、同场景）

output / `xforge` 调用 / 工具调用 / 人的动作：

| 场景 | 1.0.0 | 本次 |
|---|---|---|
| quick TT | 48911 / 78 / 169 / 9 | 26176 / 59 / 174 / 7 |
| quick FF | 64849 / 75 / 152 / 7 | 33699 / 63 / 111 / 8 |
| solid TT | 81367 / 126 / 302 / 13 | **121033 / 182 / 322 / 14** |
| solid FF | 157603 / 162 / 335 / 25 | 42947 / 106 / 213 / 10 |
| solid FF · stepwise | 293128 / 306 / 484 / 12 | 291217 / 337 / 486 / 10 |

> 注：1.0.0 那批的 oracle 是加强前的 3/4/4，本次是 5/7/7，oracle 一列不可直接比。

四降一平一升里，唯一上行的是 `solid TT`（output +49%、`xforge` +44%）。
它集中在第 3 轮：1295 秒、46 个内部轮次、47185 output，而同场另外三轮是 683/164/82 秒 ——
是实现轮自己跑长了。**新门不是原因**：新门的代价是固定的「一次写 + 一次校验」，
治理最重的 `solid FF stepwise` 会把它放得最大，而那一场对基线基本持平
（output 291217 对 293128，工具 486 对 484），外层轮还少了 3 轮、人的动作少了 2 次。
所以 `solid TT` 的上行按单场方差读。

### 新台账的真机行为

- **两场 solid（orchestrated 与 stepwise）都落了 `ledgers/exit/spec-conflicts.yaml`，内容都是 `entries: []`。**
  stepwise 那份还带了逐条核对的注释（哪几条 Requirement、为什么不构成冲突）。
- **写空不要钱。** `xforge-commands.log` 里没有 `attest entry exit/spec-conflicts`；
  出口条件 `{kind: attested}` 只对需要署名的条目要签（`src/verbs/exit.ts:88`），
  空台账一条都不需要，靠文件存在即满足。人的动作数没有因此增加。
- **门确实会咬。** stepwise 第 1–2 轮出现过 `ledger-missing: exit/spec-conflicts` ——
  站想空着出去，控制面拦下，执行者补写后继续。**在站内解决，没惊动人，也没算返工。**
- quick 流程没有 design 站，因此没有这个文件，符合预期。

### codex 那笔改动没有波及 claude-only 的树

这五场都是 `--platform claude`：`doctor` 全干净，`currentProvider()` 改成
「优先挑不需要宿主放行的」之后 claude 仍是当前宿主，没有一场被 codex 抢走。
执法 `deny` 五场全为 0（按规矩非零要逐条看，这次不用看）。

复现：`node scripts/live-matrix.mjs --engine B --concurrency 5`
