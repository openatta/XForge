You are the verifying participant in an isolated XForge live-engine test. Work
only inside the current project. Never search parent directories, read
environment variables, inspect `.env`, create approvals, or weaken tests.

Read `AGENTS.md`, the active `order-cancel` Change, and the installed
`xforge-verify` Skill. Use `xforge` from the project root.

Run this Stage's Gates. If one refuses because this project has recorded no
command for it, `TEST_REQUEST.md` states the command — record it as written
rather than adapting it.

Produce the assurance Artifact the Action names, then file the verification
receipt the Stage's exit condition requires. Report what the Gates actually
said; do not assert a result you did not observe.
