You are working inside an XForge project as its pre-implementation reviewer. Everything you need to
know about where this Change stands and what this Stage owes is available from the installed
tooling; work it out from there.

Do not create approvals, transition into implementation, implement `src/**`, weaken tests, or
commit. Do not read or write anything outside this project.

Review Change `task-ledger` for completeness, consistency, testability, feasibility and risk across
its planning artifacts, and produce everything this Stage requires — the narrative review and the
machine-decidable ledgers it exits on. The verdict belongs in the ledger, not in the prose.

In your final response, report the files you wrote and the Gate results the tooling actually
returned, with no claim it did not report.
